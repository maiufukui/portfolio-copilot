# Runtime dependencies for server.py (the deployed FastAPI backend) only.
#
# This is requirements.txt minus ragas -- ragas (and the pandas/datasets/
# etc. it pulls in) is only imported by run_eval.py, which never runs in
# production. Keeping it out of the deploy build keeps Render's build
# faster and the deployed image smaller; the full requirements.txt (with
# ragas) remains the one to use for local dev, since eval work still
# needs it.
langchain==0.3.27
langchain-community==0.3.27
langchain-openai==0.3.28
langchain-qdrant==0.2.0
langchain-text-splitters==0.3.9
langgraph==0.6.4
# app/graph.py imports PostgresSaver unconditionally at module load time
# (from langgraph.checkpoint.postgres import PostgresSaver), so this is a
# hard runtime dependency of the deployed server, not an optional extra --
# missing it here is a real, confirmed deploy failure (2026-07-28,
# ModuleNotFoundError: No module named 'langgraph.checkpoint.postgres' at
# server startup), not a hypothetical. Pin matches requirements.txt exactly
# -- see that file's own comment for why 2.0.25, not latest.
langgraph-checkpoint-postgres==2.0.25
qdrant-client==1.15.0
tiktoken==0.9.0
beautifulsoup4==4.13.4
lxml==5.3.0
pymupdf==1.25.3
python-dotenv==1.1.0
requests==2.32.3
fastapi==0.115.6
uvicorn[standard]==0.34.0
# SQLAlchemy + psycopg -- app/db.py, imported by app/tools.py at import
# time, so this is a hard runtime dependency of the deployed server, not
# an optional extra. yfinance deliberately NOT included here: it's only
# used by backfill_price_history.py, a one-time script that never runs
# in production -- same "keep the deploy image lean" reasoning already
# applied to ragas above.
SQLAlchemy==2.0.51
psycopg[binary]==3.3.4
# langchain-cohere + rank-bm25 -- item 4's parent-child reranker, now a
# hard runtime dependency: app/tools.py's search_filings imports
# parent_child_retriever, which imports both at module load time.
langchain-cohere==0.4.6
rank-bm25==0.2.2
